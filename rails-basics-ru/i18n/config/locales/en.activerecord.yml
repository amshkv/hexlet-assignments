en:
  activerecord:
    models:
      # BEGIN
      post:
        one: Post
        other: Posts
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      post:
        title: Title
        body: Body
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            post:
              title:
                blank: Title can't be blank
            # END
