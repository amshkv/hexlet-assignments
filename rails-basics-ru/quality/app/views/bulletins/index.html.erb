<h1>Bulletins</h1>
<%# BEGIN %>
<% @bulletins.each do |bulletin| %>
  <div class="bulletin">
    <h2><%= link_to bulletin.title, bulletin_path(bulletin) %></h2>
    <p><%= bulletin.body %></p>
  </div>
<% end %>
<%# END %>
